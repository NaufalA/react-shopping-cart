export default function itemModel(
  id,
  name,
  category,
  color,
  size,
  imgUrl,
  price,
  note
) {
  return {
    id,
    name,
    category,
    color,
    size,
    imgUrl,
    price,
    note,
  };
}
